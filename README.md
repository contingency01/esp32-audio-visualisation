# esp32-audio-visualisation

This project aims visualising the frequencies of input sound in real-time using an ESP32 set-up. Goertzel algorithm is used in order to select a custom set of frequencies that human ear can detect.
